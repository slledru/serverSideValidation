<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>ServerSide Validation</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
  </head>
  <body class='container-fluid'>
    <h1>Hello! Welcome to ServerSide Validation.</h1>
    <hr>
    <h3>Here are the current users:</h3>
      <table class="table table-bordered table-striped">
        <th>Username</th>
        <th>Email</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Password</th>
      {{#each userlist}}
        <tr>
          <td>{{this.username}}</td>
          <td>{{this.email}}</td>
          <td>{{this.firstname}}</td>
          <td>{{this.lastname}}</td>
          <td>{{this.password}}</td>
        </tr>
      {{/each}}
    </table>
    <br>
    <ul id="server-side-validation-errors" style="color: red;"></ul>
    {{#if error}}
      <h3>{{error}}</h3>
    {{/if}}
    <h3>Login with username and password: </h3>
    <div class="row">
      <div class="col-xs-8 col-xs-offset-2">
        <form data-toggle="validator" action="/login" id="login" method="post" role="form" class="form">
          <div class="form-group">
            <label for="login_username">Username</label>
            <input id="login_username" class="form-control" type="text" pattern="^[_A-z0-9]{1,18}$" maxlength="18" data-error="No special characters or spaces. Maximum length of 18! " name="username" value="{{user.username}}" required>
            <div class="help-block with-errors"></div>
          </div>
          <div class="form-group">
            <label for="login_password">Password</label>
            <input id="login_password" class="form-control" type="password" pattern=".{10,}" data-error="Password must be at least 10 characters." name="password" required>
            <div class="help-block with-errors"></div>
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>
      </div>
    </div>
    <h3>Sign up here to become a user: </h3>
    <div class="row">
      <div class="col-xs-8 col-xs-offset-2">
        <form data-toggle="validator" action="/signup" id="signup" method="post" role="form" class="form">
          <div class="form-group">
            <label for="username">Username</label>
            <input id="username" class="form-control" type="text" pattern="^[_A-z0-9]{1,18}$" maxlength="18" data-error="No special characters or spaces. Maximum length of 18! " name="username" value="{{user.username}}" required>
            <div class="help-block with-errors"></div>
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input id="inputEmail" class="form-control" type="email" data-error="Is that really an email?" name="email" value="{{user.email}}" required>
            <div class="help-block with-errors"></div>
          </div>
          <div class="form-group">
            <label for="firstname">First name</label>
            <input id="firstname" class="form-control" type="text" pattern="^[_A-z]{1,30}$" maxlength="30" data-error="Letters only, and a max length of 30, please!" name="firstname" value="{{user.firstname}}" required>
            <div class="help-block with-errors"></div>
          </div>
          <div class="form-group">
            <label for="lastname">Last name</label>
            <input id="lastname" class="form-control" type="text" pattern="^[_A-z]{1,30}$" maxlength="30" data-error="Letters only, and a max length of 30, please!" name="lastname" value="{{user.lastname}}" required>
            <div class="help-block with-errors"></div>
          </div>
          <div class="form-group">
            <label for="password">Password</label>
            <input id="password" class="form-control" type="password" pattern=".{10,}" data-error="Password must be at least 10 characters." name="password" required>
            <div class="help-block with-errors"></div>
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>
      </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-0mSbJDEHialfmuBBQP6A4Qrprq5OVfW37PRR3j5ELqxss1yVqOtnepnHVP9aJ7xS" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/1000hz-bootstrap-validator/0.11.5/validator.js"></script>
    <script src="/javascripts/frontend.js"></script>
  </body>
</html>
